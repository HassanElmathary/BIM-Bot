rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Lock down all direct client access.
    // All read/write operations must go through Cloud Functions using the Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
